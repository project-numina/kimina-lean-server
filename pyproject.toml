[project]
name = "kimina"
version = "0.1.0"
requires-python = ">=3.13"

[tool.uv]
dev-dependencies = ["pre-commit>=4.2.0"]

[tool.uv.sources]
server = { path = "server" }
client = { path = "client" }

